"""Utilities for reproducing the selected manuscript figure data.

The scripts in ``../scripts`` intentionally read curated CSV/NPZ inputs from
``data/raw`` and write small, per-subfigure CSV files to ``outputs``.  The
expensive simulation implementations are preserved separately in
``source_code`` for provenance and regeneration.
"""

