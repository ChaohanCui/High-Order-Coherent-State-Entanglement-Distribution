You are helping me reproduce the manuscript figures from this GitHub repository.

Goal:
Reproduce the manuscript data tables and generate clean publication-style plots for the included figures.

Repository context:
This repo contains a curated release package for coherent-state-assisted QAM entanglement-distribution simulations. The main scripts are under `scripts/`, curated raw data are under `data/raw/`, and source-code provenance is under `source_code/`.

Please do the following:

1. Read `README.md` and `DATA_MANIFEST.md` first.
2. Install the Python requirements from `requirements.txt`.
3. Run:

   python scripts/validate_release.py

   Confirm that validation passes.
4. Run:

   python scripts/make_all_subfigure_data.py

   This should regenerate CSV files under `outputs/subfigure_data/`.
5. Inspect the generated CSV files and create plotting scripts under a new folder, for example `plotting/`.
6. Generate plots for:

   - Fig. 1(c): hashing bound vs channel loss for QAM SRM.
   - Fig. 2(a): optimized spacing `d` vs channel loss.
   - Fig. 2(b): hashing bound vs `d` at selected losses.
   - Fig. 3: interface-loss and phase-bias imperfection scans.
   - End Matter Fig. 5(a): raw-label/full-state SRM vs vacuum-omit SRM.
   - End Matter Fig. 5(b): raw-label/full-state SRM vs optimized 32-outcome POVM.

7. Use the generated CSVs as the plotting source. Do not manually edit or round numerical values.
8. For the optimized POVM comparison, use the data generated by `scripts/get_fig5b_data.py` and the saved NPZ metadata as handled by the repository scripts.
9. Save plots under `outputs/figures/` in both `.png` and `.pdf` formats.
10. At the end, give me:
    - the commands you ran,
    - which output files were created,
    - whether validation passed,
    - and any missing external baseline data.

Important:
Fig. 1(c) may include external baselines such as PLOB, CTW, single-photon, or Hex-GKP curves. If those baseline numeric files are not present in `data/raw/external_baselines/`, do not invent them. Plot only the included QAM SRM data and clearly state that external baselines require separate input data.

Use clean matplotlib styling suitable for a physics manuscript:
- readable labels,
- legends with QAM order,
- consistent colors across figures,
- log scale only where appropriate or requested by the data description,
- no unnecessary decoration.

